{% extends 'base.html.twig' %}

{% block title %}Produto{% endblock %}

{% block body %}
    <h1>Produto</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ produto.id }}</td>
            </tr>
            <tr>
                <th>Descrição</th>
                <td>{{ produto.Descrição }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_produto_index') }}" class="btn btn-primary"><i class="fa fa-arrow-left"></i></a>

    <a href="{{ path('app_produto_edit', {'id': produto.id}) }}" class="btn btn-secondary"><i class="fas fa-pencil"></i></a>

    {{ include('produto/_delete_form.html.twig') }}
{% endblock %}
