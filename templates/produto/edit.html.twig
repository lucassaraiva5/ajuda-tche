{% extends 'base.html.twig' %} {% block title %}Editar Produto{% endblock %} {%
block body %}
<div class="container mt-5 p-5">
  <div class="d-flex justify-content-between align-items-center mb-2">
    <h1 class="font-weight-bold">Editar Produto</h1>
    <div class="d-flex">
      <a
        href="{{ path('app_produto_index') }}"
        class="btn btn-primary shadow m-1"
        ><i class="fa fa-arrow-left"></i> Voltar</a
      >
      {{ include("produto/_delete_form.html.twig") }}
    </div>
  </div>

  <div class="card shadow-lg">
    <div class="card-body">
      {{ include("produto/_form.html.twig", { button_label: "Atualizar" }) }}
    </div>
  </div>
</div>
{% endblock %}
