<form method="post" action="{{ path('app_produto_delete', {'id': produto.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ produto.id) }}">
    <button class="btn btn-danger"> <i class="fas fa-trash"></i></button>
</form>
