{% extends 'base.html.twig' %} {% block title %}Editar Motorista{% endblock %}
{% block body %}
<div class="container mt-5">
  <div class="d-flex justify-content-between align-items-center mb-2">
    <h1 class="font-weight-bold">Editar Motorista</h1>
    <div class="d-flex">
      <a
        href="{{ path('app_motorista_index') }}"
        class="btn btn-primary shadow m-1"
        ><i class="fa fa-arrow-left"></i> Voltar</a
      >
      {{ include("motorista/_delete_form.html.twig") }}
    </div>
  </div>

  <div class="card shadow-lg">
    <div class="card-body">
      {{ include("motorista/_form.html.twig", { button_label: "Atualizar" }) }}
    </div>
  </div>
</div>
{% endblock %}
