{% extends 'base.html.twig' %}

{% block title %}ProdutoPosto{% endblock %}

{% block body %}
{# <h1>ProdutoPosto</h1>

<table class="table">
    <tbody>
        <tr>
            <th>Id</th>
            <td>{{ produto_posto.id }}</td>
        </tr>
        <tr>
            <th>Quantidade</th>
            <td>{{ produto_posto.quantidade }}</td>
        </tr>
    </tbody>
</table> #}

{# <a href="{{ path('app_produto_posto_index') }}" class="btn btn-primary"><i class="fa fa-arrow-left"></i></a>

<a href="{{ path('app_produto_posto_edit', {'id': produto_posto.id}) }}" class="btn btn-secondary"><i
        class="fas fa-pencil"></i></a> #}


<div class="container mt-5 p-5">
    <div class="d-flex justify-content-between align-items-center mb-2">
        <h2>Produto Posto</h2>
        <div class="d-flex gap-2">
            <a href="{{ path('app_produto_posto_index') }}" class="btn btn-primary"><i class="fa fa-arrow-left"></i></a>

            <a href="{{ path('app_produto_posto_edit', {'id': produto_posto.id}) }}" class="btn btn-secondary"><i
                    class="fas fa-pencil"></i></a>
            {{ include('produto_posto/_delete_form.html.twig') }}

        </div>
    </div>

    <div class="card shadow-lg">
        <div class="card-body">
            <table class="table">
                <tbody>
                    <tr>
                        <th>Id</th>
                        <td>{{ produto_posto.id }}</td>
                    </tr>
                    <tr>
                        <th>Descrição</th>
                        <td>{{ produto_posto.quantidade }}</td>
                    </tr>
                </tbody>
            </table>
        </div>
    </div>
</div>
{% endblock %}