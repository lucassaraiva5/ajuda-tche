{% extends 'base.html.twig' %}

{% block title %}PostoAjuda{% endblock %}

{% block body %}
    <h1>PostoAjuda</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ posto_ajuda.id }}</td>
            </tr>
            <tr>
                <th>Descricao</th>
                <td>{{ posto_ajuda.descricao }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_posto_ajuda_index') }}">back to list</a>

    <a href="{{ path('app_posto_ajuda_edit', {'id': posto_ajuda.id}) }}">edit</a>

    {{ include('posto_ajuda/_delete_form.html.twig') }}
{% endblock %}
