<form method="post" action="{{ path('app_unidade_conversao_delete', {'id': unidade_conversao.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ unidade_conversao.id) }}">
    <button class="btn">Delete</button>
</form>
