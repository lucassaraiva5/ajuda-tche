{% extends 'base.html.twig' %}

{% block title %}Edit Categoria{% endblock %}

{% block body %}


<div class="container mt-5 p-5">
    <div class="d-flex justify-content-between align-items-center mb-2">
        <h2>Edit Categoria</h1>
            <div class="d-flex gap-2">
                <a href="{{ path('app_categoria_index') }}" class="btn btn-primary"><i class="fa fa-arrow-left"></i> Voltar</a>
                {{ include('categoria/_delete_form.html.twig') }}
            </div>
    </div>

    <div class="card shadow-lg">
        <div class="card-body">
            {{ include('categoria/_form.html.twig', {'button_label': 'Atualizar'}) }}
        </div>
    </div>
</div>
{% endblock %}