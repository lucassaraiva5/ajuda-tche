{% extends 'base.html.twig' %}

{% block title %}Nova Categoria{% endblock %}

{% block body %}


{# <a href="{{ path('app_categoria_index') }}" class="btn btn-primary"><i class="fa fa-arrow-left"></i></a> #}


<div class="container mt-5 p-5">
    <div class="d-flex justify-content-between align-items-center mb-2">
        <h2>Nova Categoria</h2>
        <a href="{{ path('app_categoria_index') }}" class="btn btn-primary shadow"><i class="fa fa-arrow-left"></i>
            Voltar</a>
    </div>
    <div class="card shadow-lg">
        <div class="card-body">
            {{ include('categoria/_form.html.twig') }}
        </div>
    </div>
</div>
{% endblock %}