{% extends 'base.html.twig' %}

{% block title %}UnidadeArmazenamento index{% endblock %}

{% block body %}
    <h1>UnidadeArmazenamento index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Descricao</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for unidade_armazenamento in unidade_armazenamentos %}
            <tr>
                <td>{{ unidade_armazenamento.id }}</td>
                <td>{{ unidade_armazenamento.descricao }}</td>
                <td>
                    <a href="{{ path('app_unidade_armazenamento_show', {'id': unidade_armazenamento.id}) }}">show</a>
                    <a href="{{ path('app_unidade_armazenamento_edit', {'id': unidade_armazenamento.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_unidade_armazenamento_new') }}">Create new</a>
{% endblock %}
